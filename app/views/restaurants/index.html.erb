<h1>Discover our amazing restaurants !!!</h1>

<div>
  <ul>
    <% @restaurants.each do |restaurant| %>
    <li><%= link_to restaurant.name, restaurant_path(restaurant.id) %></li>
    <% end %>
  </ul>
</div>

<%= link_to 'Create a restaurant', new_restaurant_path %>
